import {myabstract} from './MyAbstract'
export class myabstract1 extends myabstract {
    
    
    add(a: number, b: number) {
        console.log(`
        Addition is ${(a+b)}
        `);
    }

    
}